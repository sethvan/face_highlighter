#include "faces.h"
#include "glm/glm.hpp"
#include "pickloader.h"
#include "stl_io.h"
#include <emscripten.h>
#include <emscripten/bind.h>
#include <emscripten/val.h>
#include <iostream>
#include <vector>

using namespace emscripten;

int main()
{

    //  seth_tl::stl_data info;
    //  seth_tl::parse_stl( info, stl_file_name );
    //  seth_tl::getAdjacentFaces( info.triangles );
    //  std::vector<seth_tl::Triangle> triangles = info.triangles;
    //  seth_tl::Point model_center = seth_tl::getCenter( triangles );
    //  float scaleFactor = seth_tl::getScaleFactor( triangles );
    //  std::vector<std::vector<seth_tl::Snippet>> selectedIndices;
    //  selectedIndices.push_back( {} ); // Do not want to use an index 0
    // getFloat();

    return 0;
}